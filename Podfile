# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'statapp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  #pod 'iosMath'
  #pod 'RichTextView'
  pod 'SwiftyStats'

  target 'statappTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'statappUITests' do
    # Pods for testing
  end

end
